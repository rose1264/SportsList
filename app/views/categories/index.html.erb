<center>
  <div class='wrapper'>
    <div class='container'>
      <%= flash[:notice] %>
      <h3>Categories</h3>
      <% @categories.each do |category|%>
        <p class="category"><%= link_to category.name, products_url(:category => category.name)%></p>
      <% end %>
    </div>
  </div>
</center
